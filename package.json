{
  "name": "playwright-recorder",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0",
    "ffmpeg-static": "^5.1.0"
  }
}
